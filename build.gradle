apply plugin: 'java'
apply plugin: 'maven'

group = 'com.mkyong'
def majorVersion = '1.0.'
version = project.hasProperty('release') ? majorVersion+project['release'] : majorVersion+'0'

description = """NumberGenerator"""

sourceCompatibility = 1.6
targetCompatibility = 1.6



repositories {
        
     maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    testCompile group: 'junit', name: 'junit', version:'4.11'
}
